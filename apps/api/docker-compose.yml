version: '3.8'

services:
  api:
    build:
      context: ../..
      dockerfile: apps/api/Dockerfile
    ports:
      - "3001:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
    # Add your env vars here or use .env file
    # Uncomment and create .env file with your variables:
    # env_file:
    #   - .env





